name: Auto-Update Declarations Index

on:
  push:
    paths:
      - "Governance/SOPs/**"
      - ".github/workflows/update-CAM-SOP-Index.yml"
      - "Scripts/update-CAM-SOP-Index.py"
  pull_request:
    paths:
      - "Governance/SOPs/**"

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # required for committing changes
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run index updater
        run: |
          python -m pip install --upgrade pip
          python Scripts/update-CAM-SOP-Index.py

      - name: Commit changes (if any)
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "[AUTO][Declarations] Updated CAM-SOP-Index.md (sorted, summaries)"
          file_pattern: Governance/SOPs/CAM-SOP-Index.md
